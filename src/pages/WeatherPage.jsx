import React from 'react';
import WeatherApp from '../weather/WeatherApp';

const WeatherPage = () => {
    return (
        <div>
            <WeatherApp />
        </div>
    );
};

export default WeatherPage;
